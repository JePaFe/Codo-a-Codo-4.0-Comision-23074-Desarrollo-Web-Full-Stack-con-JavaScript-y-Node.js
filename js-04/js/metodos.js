const numeros = [3, 12, 6, 51, 8, 5, 6];

// const numero = numeros.find((numero) => numero == 6);
// const index = numeros.findIndex((numero) => numero == 6);

// const numeros_filtrados = numeros.filter((numero) => numero >= 10);
// const numeros_map = numeros.map((numero) => numero ** 2);

// const todos = numeros.every((numero) => numero > 18 || numero == 18);
// const algunos = numeros.some((numero) => numero >= 18);

// numeros.forEach((numero) => console.log(numero));

// for (let i = 0; i < numeros.length; i++) {
//   console.log(numeros[i]);
// }
